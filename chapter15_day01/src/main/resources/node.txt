命令模式:Encapsulate a request as an object,thereby letting you 
	parameterize clients with different requests,queues,or log 
	requests and support undoable(可撤销的) operations
	
	将一个请求封装成一个对象，从而让你让你使用不同的请求把客户端参数化，对请求
队列或者请求日志，可以提供命令的撤销和恢复功能。